#!/usr/bin/env node

const help = `plugin-gentleman CLI

Mustachi model configuration is temporarily disabled.

The plugin now uses stable local phrases only; no model/session prompt
configuration is applied from this CLI.
`

console.log(help)
